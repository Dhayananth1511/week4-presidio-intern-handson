/**
 * Simple In-Memory Task Database
 * Associates a list of tasks with a user's email.
 */
const tasks = [
  // Example structure:
  // { userEmail: 'admin@test.com', id: '1', title: 'Buy milk', status: 'To Do', priority: 'Low' }
];

module.exports = tasks;
